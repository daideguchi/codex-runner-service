services:
  - type: web
    name: codex-runner
    env: python
    plan: starter
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    autoDeploy: true
